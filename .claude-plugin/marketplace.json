{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "neo-plugins",
  "description": "Eugen's local Claude Code plugins.",
  "owner": {
    "name": "Eugen Luchian"
  },
  "plugins": [
    {
      "name": "fullstack-sync",
      "description": "Keep a client app and its backend API in sync: detect/fix endpoint-contract drift, scaffold endpoints across both repos, run and verify both sides. Adds parallel two-session contract sync (fingerprint slices + drift detection) and a safe-by-default sync-level review nudge (off/low/hard). Built for Flutter + FastAPI, stack-agnostic.",
      "source": "./",
      "category": "development"
    }
  ]
}
